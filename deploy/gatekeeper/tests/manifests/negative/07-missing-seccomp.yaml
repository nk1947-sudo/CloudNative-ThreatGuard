apiVersion: v1
kind: Pod
metadata:
  name: insecure-07-missing-seccomp
  namespace: threatguard
  labels:
    test: admission-negative-07
spec:
  securityContext:
    runAsNonRoot: true
    runAsUser: 10001
  containers:
    - name: web
      image: cgr.dev/chainguard/python:latest
      securityContext:
        privileged: false
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 10001
        capabilities:
          drop:
            - ALL
        # VIOLATION: seccompProfile is missing
